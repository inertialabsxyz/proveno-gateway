//! The agent-facing MCP server: `execute`, `check` and the lua-guide.

// Phase 4a stub

use crate::config::GatewayConfig;

pub async fn serve(_config: GatewayConfig) -> anyhow::Result<()> {
    todo!("Phase 4a")
}
